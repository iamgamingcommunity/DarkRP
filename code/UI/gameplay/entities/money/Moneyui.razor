@using Sandbox;
@using Sandbox.UI;
@inherits PanelComponent
@namespace Sandbox

<root>
	<div class="money">
	<p>@MoneyStackUIVar</p>
	
	</div>
</root>

@code
{

	[Property] public int MoneyStackUIVar { get; set; }

	/// <summary>
	/// the hash determines if the system should be rebuilt. If it changes, it will be rebuilt
	/// </summary>
	protected override int BuildHash() 
	{
		return System.HashCode.Combine( MoneyStackUIVar );
	}
}